# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB Configuration
# For local MongoDB instance
MONGODB_URI=mongodb://localhost:27017/task-management-api

# For MongoDB Atlas (cloud database)
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority

# JWT Configuration
# IMPORTANT: Generate strong secrets in production
# You can generate random secrets using: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

# Access Token Secret (used for short-lived access tokens)
JWT_ACCESS_SECRET=your-super-secret-access-token-key

# Refresh Token Secret (used for long-lived refresh tokens)
JWT_REFRESH_SECRET=your-super-secret-refresh-token-key

# Token Expiration
# Format: Xs (seconds), Xm (minutes), Xh (hours), Xd (days)
# Examples: 30s, 15m, 1h, 7d
JWT_ACCESS_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration (optional - uncomment if needed)
# CORS_ORIGIN=http://localhost:3000,http://localhost:5173
